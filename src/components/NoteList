import React from "react";
import Note from "./Note";

    function NoteList({note}) {
      return (<div>
            {notes.map(note => <Note key={note.id} note={note} date={note.date} />)}

        </div>);
    }
  
  export default NoteList